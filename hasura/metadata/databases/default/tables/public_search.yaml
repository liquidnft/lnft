table:
  schema: public
  name: search
select_permissions:
  - role: anonymous
    permission:
      columns:
        - s
        - type
        - id
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - s
        - type
      filter: {}
