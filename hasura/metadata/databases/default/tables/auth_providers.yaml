table:
  name: providers
  schema: auth
array_relationships:
- name: account_providers
  using:
    foreign_key_constraint_on:
      column: auth_provider
      table:
        name: account_providers
        schema: auth
