FROM node:14-alpine
ARG NODE_ENV=production
ENV NODE_ENV $NODE_ENV
ENV HASURA_URL $HASURA_URL
ENV ESPLORA_URL $ESPLORA_URL
ENV SECRET $SECRET

WORKDIR /app

COPY . ./
RUN yarn install

CMD ["yarn", "start"]
