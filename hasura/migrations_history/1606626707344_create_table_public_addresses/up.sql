CREATE TABLE "public"."addresses"("address" text NOT NULL, "user_id" uuid NOT NULL, PRIMARY KEY ("address") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("address"));
